.main-wrapper
  .main-inner
    .main-image
      = image_tag "モロッコ１.jpg"

- @post.each do |post|
  = post.user.name
  = post.title
  = post.content
  - post.images.each do |image|
    = link_to (image_tag image.src.url), post_path(post.id)