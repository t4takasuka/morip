.album.py-4
  .container
    .row
      .col-lg-4.col-md-6.mb-3
        %h5 #{icon("fas", "users")} ユーザー 一覧
    - @users.each do |user|
      .card.user.mb-4
        .card-body
          .row
            .col-sm-2
              .user-img-box
                - if user.image?
                  = image_tag user.image.url, class: "user-img img-fliud"
                - else 
                  = image_tag "/assets/default.png", alt: "testuser", class: "user-img img-fliud"
            .col-sm-10
              .row
                .col-sm-12.col-md-12.py-1.border-bottom.border-secondary
                  = icon("fas", "user")
                  = link_to "#{user.name}", user_path(user.id) ,class: "name"
              .row.no-gutters
                .col-sm-12.col-md-12.pt-1
                  = "#{user.profile}"